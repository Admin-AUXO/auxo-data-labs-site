---
interface Props {
  variant?: "grid" | "glow" | "aurora";
  class?: string;
}

const { variant = "grid", class: className = "" } = Astro.props;
---

<div class={`section-decoration ${className}`.trim()} data-variant={variant} aria-hidden="true">
</div>
