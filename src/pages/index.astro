---
import { Icon } from "astro-icon/components";
import BaseLayout from "../layouts/BaseLayout.astro";
import Section from "../components/layout/Section.astro";
import SectionHeader from "../components/ui/SectionHeader.astro";
import Button from "../components/ui/Button.astro";
import HighlightText from "../components/ui/HighlightText.astro";
import CtaSection from "../components/sections/shared/CtaSection.astro";
import SelfCheckCta from "../components/sections/shared/SelfCheckCta.astro";
import { homeContent as c } from "../data/home";
---

<BaseLayout
  title="Analytics & AI lab for Gulf real estate"
  description="AUXO Data Labs turns the data Gulf real-estate businesses already hold into reporting, forecasts, and automation they can trust — built to last, and yours to keep."
>
  <Section padding="xl" width="7xl">    <div class="flex flex-col gap-8">
      <h1 class="text-display max-w-[15ch]" data-enter="headline">
        <HighlightText text={c.hero.title} highlight={c.hero.titleHighlight} />
      </h1>
      <p class="lead max-w-2xl text-xl" data-enter>{c.hero.subtitle}</p>
      <div class="flex flex-col gap-3 sm:flex-row sm:flex-wrap sm:items-center" data-enter>
        <Button variant="primary" size="lg" href={c.hero.primaryCta.href} icon class="w-full sm:w-auto">
          {c.hero.primaryCta.text}
        </Button>
        <Button variant="secondary" size="lg" href={c.hero.secondaryCta.href} class="w-full sm:w-auto">
          {c.hero.secondaryCta.text}
        </Button>
      </div>
    </div>
  </Section>

  <Section padding="lg" width="7xl">
    <SectionHeader
      title={c.capabilities.title}
      highlight={c.capabilities.titleHighlight}
      subtitle={c.capabilities.subtitle}
    />
    <div class="mt-12 grid gap-px border border-border bg-border sm:grid-cols-2 lg:grid-cols-4">
      {
        c.capabilities.items.map((item, i) => (
          <article class="group flex flex-col bg-surface p-8" data-reveal style={`--i:${i}`}>
            <div class="mb-6 flex size-12 items-center justify-center rounded-lg bg-accent-soft text-accent transition-colors duration-300 group-hover:bg-accent group-hover:text-accent-foreground">
              <Icon name={item.icon} class="size-6 transition-transform duration-300 group-hover:scale-110" aria-hidden="true" />
            </div>
            <h3 class="text-xl font-bold sm:min-h-[2.8em]">{item.name}</h3>
            <p class="mt-2 text-muted">{item.desc}</p>
          </article>
        ))
      }
    </div>
  </Section>

  <SelfCheckCta />

  <Section padding="md" width="7xl">
    <div class="grid gap-12 lg:grid-cols-[0.9fr_1.1fr]">
      <SectionHeader title={c.why.title} highlight={c.why.titleHighlight} />
      <ul class="flex flex-col" data-reveal>
        {
          c.why.items.map((item, i) => (
            <li
              class:list={[
                "flex flex-col gap-1 py-6 sm:flex-row sm:gap-8",
                i > 0 && "border-t border-border",
              ]}
            >
              <h3 class="text-lg font-bold sm:w-56 sm:shrink-0">{item.title}</h3>
              <p class="text-muted">{item.desc}</p>
            </li>
          ))
        }
      </ul>
    </div>
  </Section>

  <Section padding="lg" width="7xl">
    <SectionHeader title={c.how.title} highlight={c.how.titleHighlight} subtitle={c.how.subtitle} />
    <div class="mt-12 grid gap-x-8 gap-y-10 sm:grid-cols-2 lg:grid-cols-4">
      {
        c.how.steps.map((step, i) => (
          <div
            class="group flex flex-col gap-3 pt-5"
            data-anim="rise"
            style={`--i:${i}`}
          >
            <span class="mb-2 block h-0.5 w-full origin-left bg-foreground" data-anim="draw" style={`--i:${i}`} aria-hidden="true" />
            <div class="flex items-center justify-between">
              <span class="font-mono text-sm font-semibold tabular-nums text-accent-2">0{i + 1}</span>
              <Icon name={step.icon} class="size-6 text-accent-2 transition-transform duration-300 group-hover:scale-110" aria-hidden="true" />
            </div>
            <h3 class="text-2xl font-bold">{step.title}</h3>
            <p class="text-muted">{step.desc}</p>
          </div>
        ))
      }
    </div>
  </Section>

  <Section padding="lg" width="7xl">
    <div class="flex max-w-3xl flex-col gap-4" data-reveal>
      <SectionHeader title={c.outcomes.title} highlight={c.outcomes.titleHighlight} />
      <p class="lead max-w-2xl">{c.outcomes.lead}</p>
    </div>
    <ol class="mt-10 grid gap-x-16 gap-y-8 sm:mt-12 sm:grid-cols-2">
      {
        c.outcomes.items.map((item, i) => (
          <li class="flex gap-4 border-t border-border pt-6" data-anim="rise" style={`--i:${i}`}>
            <span class="mono-datum pt-1 tabular-nums text-accent-2">
              {String(i + 1).padStart(2, "0")}
            </span>
            <div class="flex flex-col gap-1.5">
              <h3 class="text-lg font-bold sm:text-xl">{item.title}</h3>
              <p class="text-muted">{item.desc}</p>
            </div>
          </li>
        ))
      }
    </ol>
  </Section>

  <CtaSection
    title={c.cta.title}
    titleHighlight={c.cta.titleHighlight}
    description={c.cta.description}
    ctaText={c.cta.ctaText}
    ctaHref={c.cta.ctaHref}
    secondaryText={c.cta.secondaryText}
    secondaryHref={c.cta.secondaryHref}
    reassurance={c.cta.reassurance}
  />
</BaseLayout>
